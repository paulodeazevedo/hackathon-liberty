
<table>
    <tr>
        <td>
            @*FORMULARIO*@
            @using (Html.BeginForm("Index", "Cliente"))
            {
                <input type="submit" id="Formulario" class="btn btn-primary" value="Formulario" />
            }
        </td>
        <td>
            @*LEAD PENDENTE*@
            @using (Html.BeginForm("Index", "AtendimentoLead"))
            {
                <input type="submit" id="pendente" class="btn btn-primary" value="Lead Pendente" />
            }
        </td>
        <td>
            @*LEAD EM ANDAMENTO*@
            @using (Html.BeginForm("AtendimentoEmAndamento", "AtendimentoLead"))
            {
                <input type="submit" id="pendente" class="btn btn-primary" value="Lead em andamento" />
            }
        </td>
        <td>
            @*LEAD CONCLUIDA*@
            @using (Html.BeginForm("AtendimentosConcluidos", "AtendimentoLead"))
            {
                <input type="submit" id="concluida" class="btn btn-primary" value="Lead Concluído" />
            }
        </td>
        <td>
            @*GESTÃO*@
            @using (Html.BeginForm("Index", "Gerenciamento"))
            {
                <input type="submit" id="pendente" class="btn btn-primary" value="Tela de Gestão" />
            }
        </td>
    </tr>
</table>